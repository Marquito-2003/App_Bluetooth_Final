<resources>

    <!-- Definindo um tema customizado para o app -->
    <style name="Theme.AppBluetoothFinal" parent="@style/Theme.AppCompat.Light">
        <!-- Cor de fundo do app -->
        <item name="colorPrimary">@color/MainLight</item>
        <item name="backgroundColor">@color/MainLight</item>
        <!-- Cor do texto e ícones do app -->
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">@color/MainLight</item>
        <item name="android:colorBackground">@color/BgLight</item>
        <item name="android:colorButtonNormal">@color/MainLight</item>
        <item name="android:textColor">@color/white</item>
        <!-- Definindo a cor do status bar -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <!--não implementado ainda! -->
    <style name="Theme.AppBluetooth_Dark" parent="@style/Theme.AppCompat.DayNight">
        <!-- Cor de fundo do app -->
        <item name="colorPrimary">@color/MainLight</item>
        <item name="backgroundColor">@color/MainLight</item>
        <!-- Cor do texto e ícones do app -->
        <item name="colorOnSecondary">@color/black</item>
        <item name="colorAccent">@color/MainLight</item>
        <item name="android:colorBackground">@color/BgLight</item>
        <item name="android:colorButtonNormal">@color/MainLight</item>
        <item name="android:textColor">@color/white</item>
        <!-- Definindo a cor do status bar -->
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

</resources>
